# OS / editor cruft
.DS_Store
*.swp

# SDLC worktrees (created by /sdlc-task and /sdlc-block at runtime)
trees/

# Hook session logs (ephemeral)
.claude/logs/

# SDLC runtime phase-state breadcrumb (D27 — regenerated each run; crash/audit state, not a deliverable)
planning/*/sdlc/sdlc-state.json

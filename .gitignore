# OS / editor cruft
.DS_Store
*.swp

# SDLC worktrees (created by /sdlc-task and /sdlc-block at runtime)
trees/

# Hook session logs (ephemeral)
.claude/logs/

# NOTE: All SDLC state files (sdlc-run-state.json, sdlc-task-state.json, sdlc-flow-state.json,
# block-orchestration-state.json, worklog.md) are intentionally COMMITTED authoritative state — D37.
# No runtime breadcrumbs are gitignored under planning/*/sdlc/.

planning/state.json
/planning
